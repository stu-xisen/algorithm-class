package class01;

/**
 * 选择排序
 *
 * 思路：
 *      0 ~ n-1遍历，找到最小值，将最小值放在a[0]上
 *          定义min值，初始为a[0],遍历数组0 ~ n-1,如果a[i]的值比min小，则min = a[i]
 *      1 ~ n-1遍历，找到最小值，将最小值放在a[1]上
 *      2 ~ n-1遍历，找到最小值，将最小值放在a[2]上
 *      .
 *      .
 *      .
 */
public class Code01_SelectionSort {

    public static void selectionSort(int[] arr){
        for (int i = 0; i < arr.length - 1 ; i++) {

        }

    }
}
